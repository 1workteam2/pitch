import StubTab from './StubTab';
export default function BasesTab() {
  return <StubTab icon="B" label="Bases" color="text-purple-400"
    persona="Rickey Henderson"
    quote="If my uniform doesn't get dirty, I haven't done anything in the baseball game." />;
}
